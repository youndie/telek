[versions]
# The shared build conventions. One number: every sborka plugin below takes its version from here.
# The shared build conventions, and the compiler with them: the `wip` catalog this release
# publishes carries `kotlin`, so there is no Kotlin version in this file to drift from the rest
# of the portfolio.
sborka = "0.4.0.87"
dokka = "2.1.0"
kotlinxCoroutines = "1.11.0"
kotlinxSerializationJson = "1.11.0"
kotlinTelegramBot = "10.0.0"
retrofit = "2.12.0" #kotlinTelegramBot requires
tgbotapi = "35.1.0"
atomicfu = "0.33.0"
okio = "3.18.1"
mockk = "1.14.11"

[libraries]
kotlinxCoroutines = { module = "org.jetbrains.kotlinx:kotlinx-coroutines-core", version.ref = "kotlinxCoroutines" }
kotlinTelegramBot = { module = "io.github.kotlin-telegram-bot.kotlin-telegram-bot:telegram", version.ref = "kotlinTelegramBot" }
kotlinxSerializationCore = { module = "org.jetbrains.kotlinx:kotlinx-serialization-core", version.ref = "kotlinxSerializationJson" }
kotlinxSerializationJson = { module = "org.jetbrains.kotlinx:kotlinx-serialization-json", version.ref = "kotlinxSerializationJson" }
kotlinxSerializationProperties = { module = "org.jetbrains.kotlinx:kotlinx-serialization-properties", version.ref = "kotlinxSerializationJson" }
retrofitCore = { module = "com.squareup.retrofit2:retrofit", version.ref = "retrofit" }
tgbotapi = { module = "dev.inmo:tgbotapi", version.ref = "tgbotapi" }
# Used as a plain library (locks only, no `atomic()` fields), so the atomicfu compiler plugin is
# deliberately not applied — see CLAUDE.md.
atomicfu = { module = "org.jetbrains.kotlinx:atomicfu", version.ref = "atomicfu" }
okio = { module = "com.squareup.okio:okio", version.ref = "okio" }
okioFakeFileSystem = { module = "com.squareup.okio:okio-fakefilesystem", version.ref = "okio" }
kotlinxCoroutinesTest = { module = "org.jetbrains.kotlinx:kotlinx-coroutines-test", version.ref = "kotlinxCoroutines" }
mockk = { module = "io.mockk:mockk", version.ref = "mockk" }

[plugins]
sborkaKmp = { id = "io.github.youndie.sborka.kmp", version.ref = "sborka" }
sborkaJvm = { id = "io.github.youndie.sborka.jvm", version.ref = "sborka" }
sborkaLint = { id = "io.github.youndie.sborka.lint", version.ref = "sborka" }
sborkaPublish = { id = "io.github.youndie.sborka.publish", version.ref = "sborka" }
dokkaPlugin = { id = "org.jetbrains.dokka", version.ref = "dokka" }
# The Kotlin plugins are named here now. They used to arrive on the classpath through buildSrc, which
# is gone: `sborka.kmp` and `sborka.jvm` configure Kotlin and deliberately do not choose its version.
